<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/tournoi"
            android:icon="@drawable/ic_tournoi"
            android:title="Mes tournois actuels" />
        <item
            android:id="@+id/tournoi_inscription"
            android:icon="@drawable/ic_inscription"
            android:title="S'inscrire à un tournoi" />
        <item
            android:id="@+id/historique"
            android:icon="@drawable/ic_historique"
            android:title="Historique des tournois" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/suggestion"
                android:icon="@drawable/ic_menu_send"
                android:title="Suggestions" />
            <item
                android:id="@+id/contact"
                android:icon="@drawable/ic_menu_send"
                android:title="Contact" />
        </menu>
    </item>

</menu>
